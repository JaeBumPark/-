import sys
sys.stdin=open("input4.txt", "rt")
N, M=map(int, input().split())
K = list(map(int, input().split()))
K.sort() #오름차순 정렬
lt=0
rt=N-1
while lt<=rt:
  mid=(lt+rt)//2
  if K[mid]==M:
     print(mid+1)
     break
  elif K[mid]>M:
      rt=mid-1   
  else:
    lt=mid+1
