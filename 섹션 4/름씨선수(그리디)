import sys
sys.stdin=open("input4.txt", "rt")
n=int(input())
smo=[]
for i in range(n):
    l,w=map(int,input().split())
    smo.append((l,w))
smo.sort(reverse=True)    
print(smo)
hap=0
mx=0 # 최대 몸무게
for l,w in smo:
    if w>=mx:
        mx=w
        hap+=1
print(hap)        