
n= int(input())
k=map(int,input().split())
score=0
fs=0 

for i in k:
   if i == 1: #정답일 경우
      score+=1   #score에 1점 더하고
      fs+=score  #연속 정답에도 +, 최종 점수임
          
   else:
      score=0        

print(fs)        