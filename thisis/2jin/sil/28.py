N= int(input())
array = list(map(int, input().split()))

def binary_search(array, target, start, end)
    while start < end:
          mid = (start+end)//2
          if array[mid] == target:
             return mid
          elif array[mid] > mid
                